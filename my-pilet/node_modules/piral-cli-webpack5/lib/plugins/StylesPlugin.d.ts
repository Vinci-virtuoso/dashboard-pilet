import { Compiler } from 'webpack';
export default class StylesPlugin {
    private cssName;
    private entryName;
    constructor(cssName: string, entryName: string);
    apply(compiler: Compiler): void;
}
